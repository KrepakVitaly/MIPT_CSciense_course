\hypertarget{classfn}{
\section{РљР»Р°СЃСЃ fn}
\label{classfn}\index{fn@{fn}}
}


Push\_\-CPU.  




\subsection{РџРѕРґСЂРѕР±РЅРѕРµ РѕРїРёСЃР°РЅРёРµ}
Push\_\-CPU. Jne\_\-CPU.

Je\_\-CPU.

Jb\_\-CPU.

Ja\_\-CPU.

Jmp\_\-CPU.

Ret\_\-CPU.

Call\_\-CPU.

Sin\_\-CPU.

Sqrt\_\-CPU.

Pow\_\-CPU.

Div\_\-CPU.

Mul\_\-CPU.

Sub\_\-CPU.

Add\_\-CPU.

Swap\_\-CPU.

Dup\_\-CPU.

Функция, которая кладет переданное ей значение в стек. Если произошло переполнение стека, то выводится сообщение об ошибке. При этом возвращается код вызвавшей функции (см. \hyperlink{instr__set_8cpp}{Instr\_\-set.cpp}), если функция вызвана пользователем, то возвращается -\/1 (NO\_\-CALLING\_\-FUNC). !

Функция, дублирующая последний элемент в стеке.

Функция, меняющая местами два последних элемента стека.

Функция, складывающая два послених элемента в стеке, и возвращающая результат в стек

Функция, вычитающая из последнего элемента стека предпоследний и возвращающая результат в стек.

Функция, умножающая последний элемент стека на предпоследний и возвращающая результат в стек.

Функция, делящая последний элемент стека на предпоследний и возвращающая результат в стек.

Функция, возводящая предпоследний элемент стека в стпень, которая лежит в последнем элементе и возвращающая результат в стек.

Функция, извлекающая корень из последнего элемента в стеке и возвращающая результат в стек.

Функция, возвращающая синус последнего элемента в стек.

Функция, возвращающая косинус последнего элемента в стек.

Функция, вызвающая функцию описанную пользователем в своей программе, записывает адрес возврата в стек адресов (CPU\_\-Addrs\_\-Stack)

Функция, которая возвращает программу в место памяти, откуда была вызвана функция (см. Call\_\-CPU)

Функция, которая перемещает программу на указанный адрес.

Функция, которая перемещает программу на указанный адрес, если последний элемент стека больше предыдущего.

Функция, которая перемещает программу на указанный адрес, если последний элемент стека меньше предыдущего.

Функция, которая перемещает программу на указанный адрес, если последний элемент стека равен предыдущему.

Функция, которая перемещает программу на указанный адрес, если последний элемент стека не равен предыдущему. 

РћР±СЉСЏРІР»РµРЅРёСЏ Рё РѕРїРёСЃР°РЅРёСЏ С‡Р»РµРЅРѕРІ РєР»Р°СЃСЃР° РЅР°С…РѕРґСЏС‚СЃСЏ РІ С„Р°Р№Р»Рµ:\begin{DoxyCompactItemize}
\item 
\hyperlink{_c_p_u__cmd_8cpp}{CPU\_\-cmd.cpp}\end{DoxyCompactItemize}
